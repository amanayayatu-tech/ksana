# 你的任务

针对以下标的，按照你的 Decision Tree 逐关推理，输出 recommendation。

## 标的

ticker: {{ target_ticker }}
来源：K deep research_signal {{ signal_id }}
K deep 的初步判断：{{ signal_summary }}

## 市场数据

{{ market_data_yaml }}

## 已回填的 Perplexity 结果

{{ perplexity_results_yaml }}

## 历史研究记忆

{{ historical_knowledge_yaml }}

## 上游信号详情

{{ upstream_signal_full_yaml }}

## 输出 schema

{{ schema_name }}

# 严格约束

1. 输出 abstain 时必须填 abstain_reason
2. 上游 evidence_unverified_inherited 时 confidence 上限 70
3. 第一阶段试运行禁止 long / short / leverage / put / hedge
4. direction 只能是 watch / avoid / abstain；如果你的方法论倾向 long，也必须输出 watch，并在 trial_run_policy.downgrade_reason 里说明 first_phase_long_disabled
5. 所有 watch / avoid / abstain 都必须有 thesis、证据引用、缺口说明
6. 只输出 JSON
